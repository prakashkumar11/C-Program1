#include<iostream>
#include<vector>

using namespace std;

int main()
{
    vector<int> vnew;

    vnew.push_back(2);
    vnew.push_back(5);
    vnew.push_back(7);
    vnew.push_back(8);
    vnew.push_back(9);

    // delete values from vectors
    // vnew.pop_back();

    // cout <<"size of vnew : " <<vnew.size() <<endl;
    // cout <<"capacity of vnew : "<<vnew.capacity() <<endl;

    // delete values any position in vectors
    // vnew.erase(vnew.begin()+1);

    // cout <<"size of vnew : " <<vnew.size() <<endl;
    // cout <<"capacity of vnew : " <<vnew.capacity() <<endl;

    // print values of vectors
    // for (int i=0; i<vnew.size(); i++)
    //     cout <<vnew[i] <<" ";

    // insert in values of vectors
    // vnew.insert(vnew.begin()+1, 50);

    // update values in vector 
    // vnew[1] = 37;

    // print vectors
    // for (int i=0; i<vnew.size(); i++)
    // cout <<vnew[i] <<" ";

    // Remove all the elements
    // vnew.clear();
    // cout <<vnew.size() <<endl;
    // cout <<vnew.capacity() <<endl;



}