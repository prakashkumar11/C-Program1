// Declaring(Creating) Variable
// Syntax => variableName=value;

#include<stdio.h>
int main()
{
    
    // int myName=15;//integer(whole number)
    // float myFloatNum=15.45;//floating point number (decimal number)
    // char myLetter='C'; //character

    // printf("------------------Output-------------------------------\n");
    // printf("%d\n", myName);
    // printf("%f\n", myFloatNum);
    // printf("%c\n", myLetter);

    // int num=5;
    // printf("My favorite number : %d", num);

    // int myNum=15;
    // char myLetter='D';
    // printf("My favorite number is %d and My favorite letter is %c", myNum, myLetter);

    // ------------------------------------Add variable together----------------------------
    // int x=10;
    // int y=20;

    // int z=x+y;
    // printf("Add two number is :%d", z);

    // ---------------------Declaring Multiple variable and add three number--------------
    // int x=10, y=20, z=50;
    // printf("Result is : %d", x+y+z);

    // int x,y,z;
    // x=y=z=50;   //x=50, y=50, z=50--->50+50+50
    // printf("Result is %d", x+y+z); // output-->150

    // -------------------------------------Remembers it-------------------------------
    // Names can contain letters, digits and underscores
    // Names must begin with a letter or an underscore (_)
    // Names are case sensitive (myVar and myvar are different variables)
    // Names cannot contain whitespaces or special characters like !, #, %, etc.
    // Reserved words (such as int) cannot be used as names


    return 0;
}